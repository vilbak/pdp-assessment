// Issue 5: conventions — named exports via barrels, no `export default` (ADR 0003);
//      addresses the original's general non-conformance (naming / types / exports).
export { ProductDetailScreen } from './screen/ProductDetailScreen';
